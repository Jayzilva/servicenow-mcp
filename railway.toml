[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m servicenow_mcp.server_sse --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "always"
