[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m servicenow_mcp.server_sse --host=0.0.0.0 --port=$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

**Create `Procfile`** (alternative/backup):
```
web: python -m servicenow_mcp.server_sse --host=0.0.0.0 --port=$PORT